<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap" rel="stylesheet">
    
    <link rel="stylesheet" href="style.css">
    <title>My simple weather web app</title>
</head>
<body>
    <h1>Get the weather for your location 🌤️👇🏼</h1>
    <form id="myForm" method="POST">
        <label for="locationWeather">Location:</label>
        <input type="text" id="locationWeather" name="location" placeholder="Your city">
        <input id="locationWeather" type="submit" value="Get weather for location" formaction="/get-location-weather">
        <input id="randomWeather" type="submit" value="Get random weather" formaction="/get-random-weather">
    </form>
    <br>
    <% if (locals.content) { %>
        <h2>These are the wheather information</h2>
        <h3>Place</h3>
        <%= content.location.name %> in <%= content.location.region %>, <%= content.location.country %>
        <p>Coordinates: <%= content.location.lat %>, <%= content.location.lon %></p>
        <p>Local Time: <%- new Date(content.location.localtime_epoch * 1000).toLocaleTimeString("de-DE") %></p>
        <h3>Weather conditions</h3>
        <p>Overall: <%= content.current.condition.text %></p>
        <p>Temp in Celsius: <%= content.current.temp_c %></p>
        <img src=<%= icon %> alt="weather icon">

    <% } %>
</body>
</html>